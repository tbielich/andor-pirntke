---
pagination:
  data: cloudinary.folders
  size: 1
  alias: folder
  resolve: values
permalink: "{{ folder.path | slugify }}/"
layout: base-folder
keywords:
  - Fahrzeug
---
<small><a href="/fahrzeuge/">Fahrzeuge</a></small>
<h1>{{ folder.name }}</h1>
<section>
  {%- for image in folder.images -%}
  {%- if image.folder == folder.path %}
  {% thumb image.secure_url, image.asset_id %}
  {%- endif -%}
  {%- endfor -%}
</section>
